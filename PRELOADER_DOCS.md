# Glass Loader - Preloader Documentation

## Обзор

Glass Loader - это современный preloader с эффектом стекломорфизма и динамической анимацией загрузки. Компонент создает плавный переход между двумя фоновыми изображениями с отображением прогресса загрузки в процентах.

## Основные возможности

- ✨ Двойной фон с плавным переходом
- 📊 Динамический прогресс загрузки (0-100%)
- 🎬 Синхронизированные анимации текста и полос
- ⏱️ Минимальное время отображения с учетом реальной загрузки
- 🌊 Эффект glassmorphism (стекломорфизм)
- 🎨 Полностью настраиваемый дизайн

---

## Быстрый старт

### Установка

Компонент уже включен в проект. Находится в:
```
client/src/components/Preloader.tsx
```

### Использование

```tsx
import { Preloader } from "@/components/Preloader";

function App() {
  const [isLoading, setIsLoading] = useState(true);

  return (
    <>
      {isLoading && <Preloader onComplete={() => setIsLoading(false)} />}
      {/* Ваш контент */}
    </>
  );
}
```

---

## Настройка

### 1. Минимальное время загрузки

По умолчанию: **3 секунды**

Изменить в файле `Preloader.tsx`:

```tsx
// Строка ~20
const minDuration = 3000; // Время в миллисекундах
```

**Примеры:**
- 2 секунды: `const minDuration = 2000;`
- 5 секунд: `const minDuration = 5000;`
- 10 секунд: `const minDuration = 10000;`

> ⚠️ **Важно:** Это минимальное время. Если страница загружается дольше, preloader будет отображаться до полной загрузки.

### 2. Фоновые изображения

#### Расположение изображений

Поместите изображения в папку:
```
images/
  ├── image_start.png  # Начальное изображение (0-80%)
  └── image_end.png    # Конечное изображение (80-100%)
```

#### Изменение путей

В файле `Preloader.tsx` (строки 4-5):

```tsx
import startBg from "@images/image_start.png";
import endBg from "@images/image_end.png";
```

**Пример с другими именами:**
```tsx
import startBg from "@images/my-start-background.jpg";
import endBg from "@images/my-end-background.jpg";
```

#### Точка перехода между изображениями

По умолчанию переход начинается с **80%**

Изменить в строке ~60:

```tsx
animate={{ 
  opacity: progress >= 80 ? (progress - 80) / 20 : 0 
}}
```

**Примеры:**
- Переход с 50%: `progress >= 50 ? (progress - 50) / 50 : 0`
- Переход с 90%: `progress >= 90 ? (progress - 90) / 10 : 0`

### 3. Скорость анимаций

#### Скорость текста "LOADING"

По умолчанию: **2 секунды** на полный цикл

Изменить в строках ~85 и ~168:

```tsx
transition={{ 
  repeat: Infinity, 
  duration: 2,  // Скорость в секундах
  ease: "linear",
}}
```

**Примеры:**
- Быстрее: `duration: 1` (1 секунда)
- Медленнее: `duration: 5` (5 секунд)

#### Скорость светящихся полос

По умолчанию: **2 секунды**

Изменить в строках ~99 и ~182:

```tsx
transition={{ 
  repeat: Infinity, 
  duration: 2,  // Скорость в секундах
  ease: "linear",
  delay: 1  // Задержка для нижней полосы
}}
```

> 💡 **Рекомендация:** Держите `duration` текста и полос одинаковыми для синхронной анимации.

### 4. Шрифты

#### Шрифт процентов (основной текст)

Настраивается через Tailwind классы в строке ~145:

```tsx
className={cn(
  "text-6xl sm:text-7xl md:text-8xl lg:text-9xl xl:text-[12rem]",
  "font-mono font-bold tracking-tighter"
)}
```

**Изменение шрифта:**
```tsx
"font-serif"        // Serif шрифт
"font-sans"         // Sans-serif
"font-display"      // Display шрифт (если настроен)
```

**Изменение размера:**
```tsx
// Уменьшенный размер
"text-4xl sm:text-5xl md:text-6xl lg:text-7xl"

// Увеличенный размер
"text-8xl sm:text-9xl md:text-[10rem] lg:text-[14rem]"
```

#### Шрифт текста "LOADING"

В строках ~83 и ~166:

```tsx
className="font-mono text-2xl sm:text-3xl md:text-4xl tracking-[0.3em] uppercase"
```

**Изменение шрифта:**
```tsx
className="font-sans text-2xl sm:text-3xl md:text-4xl"
```

**Изменение размера:**
```tsx
// Меньше
className="font-mono text-xl sm:text-2xl md:text-3xl"

// Больше
className="font-mono text-3xl sm:text-4xl md:text-5xl"
```

### 5. Цвета и прозрачность

#### Цвет текста "LOADING"

Строки ~83 и ~166:

```tsx
className="text-white/40"  // Белый с 40% прозрачностью
```

**Примеры:**
```tsx
"text-white/60"      // Более яркий
"text-white/20"      // Более тусклый
"text-blue-400/50"   // Синий с прозрачностью
```

#### Цвет процентов

По умолчанию используется градиент (заливка снизу вверх):

```tsx
backgroundImage: `linear-gradient(to top, white ${progress}%, transparent ${progress}%)`
```

**Изменение цвета:**
```tsx
// Синий градиент
backgroundImage: `linear-gradient(to top, #60a5fa ${progress}%, transparent ${progress}%)`

// Зеленый градиент
backgroundImage: `linear-gradient(to top, #34d399 ${progress}%, transparent ${progress}%)`
```

#### Цвет светящихся полос

Строки ~96 и ~179:

```tsx
className="bg-white shadow-[0_0_15px_rgba(255,255,255,0.8)]"
```

**Примеры:**
```tsx
// Синее свечение
"bg-blue-400 shadow-[0_0_15px_rgba(96,165,250,0.8)]"

// Зеленое свечение
"bg-green-400 shadow-[0_0_15px_rgba(52,211,153,0.8)]"
```

#### Фоновое затемнение (backdrop)

Строка ~68:

```tsx
<div className="absolute inset-0 z-[10] bg-black/40 backdrop-blur-2xl" />
```

**Настройка:**
```tsx
// Темнее
"bg-black/60 backdrop-blur-2xl"

// Светлее
"bg-black/20 backdrop-blur-2xl"

// Без размытия
"bg-black/40"

// Сильнее размытие
"bg-black/40 backdrop-blur-3xl"
```

### 6. Интервал обновления прогресса

По умолчанию: **30 миллисекунд**

Изменить в строке ~19:

```tsx
const intervalTime = 30; // В миллисекундах
```

**Примеры:**
- Плавнее (чаще): `const intervalTime = 16;` (~60 FPS)
- Экономичнее (реже): `const intervalTime = 50;`

> ⚠️ Слишком малое значение может повлиять на производительность на слабых устройствах.

### 7. Время выхода анимации

Время, за которое preloader исчезает после 100%:

```tsx
// Строка ~36
setTimeout(onComplete, 800); // 800ms
```

**Примеры:**
- Быстрее: `setTimeout(onComplete, 400);`
- Медленнее: `setTimeout(onComplete, 1200);`

---

## Дополнительные эффекты

### Noise (шум)

Эффект зернистости поверх изображения (строка ~71):

```tsx
<div className="absolute inset-0 noise-bg z-[51] opacity-30 mix-blend-overlay" />
```

**Настройка интенсивности:**
```tsx
"opacity-10"  // Слабый шум
"opacity-50"  // Сильный шум
"opacity-0"   // Отключить шум
```

### Эффект исчезновения текста "LOADING"

Текст исчезает с 80% прогресса:

```tsx
animate={{ 
  opacity: progress >= 80 ? Math.max(0, 1 - (progress - 80) / 20) : 1 
}}
```

**Изменить точку исчезновения:**
```tsx
// Исчезает с 90%
opacity: progress >= 90 ? Math.max(0, 1 - (progress - 90) / 10) : 1

// Исчезает с 70%
opacity: progress >= 70 ? Math.max(0, 1 - (progress - 70) / 30) : 1
```

---

## Типичные сценарии

### Сценарий 1: Быстрый preloader (1 секунда)

```tsx
// В Preloader.tsx, строка ~20
const minDuration = 1000;

// Строки ~85, ~168 - ускорить анимации
duration: 1
```

### Сценарий 2: Долгий cinematic preloader (10 секунд)

```tsx
// В Preloader.tsx, строка ~20
const minDuration = 10000;

// Строки ~85, ~168 - замедлить анимации
duration: 5
```

### Сценарий 3: Минималистичный (без текста LOADING)

Удалить или закомментировать блоки в строках:
- ~76-108 (верхний блок)
- ~154-190 (нижний блок)

### Сценарий 4: Использование собственного логотипа вместо процентов

Заменить блок в строках ~112-152:

```tsx
<div className="relative z-[55] flex flex-col items-center">
  <motion.img
    src="/path/to/your/logo.png"
    alt="Loading"
    initial={{ scale: 0.8, opacity: 0 }}
    animate={{ scale: 1, opacity: 1 }}
    className="w-48 h-48"
  />
  <p className="mt-4 text-white font-mono">{progress}%</p>
</div>
```

---

## Решение проблем

### Preloader не отображается

**Проверьте:**
1. Правильность путей к изображениям
2. Наличие файлов `image_start.png` и `image_end.png` в папке `images/`
3. Настройку алиаса `@images` в `vite.config.ts`

### Preloader завис на 100%

**Причина:** Не вызывается `onComplete`

**Решение:**
```tsx
// Проверьте, что передаете функцию в onComplete
<Preloader onComplete={() => setIsLoading(false)} />
```

### Анимации не синхронизированы

**Убедитесь**, что `duration` одинаковый для:
- Текста LOADING (строки ~85 и ~168)
- Светящихся полос (строки ~99 и ~182)

```tsx
// Везде должно быть одно значение
duration: 2
```

### Preloader слишком быстро исчезает

**Увеличьте минимальное время:**

```tsx
const minDuration = 5000; // 5 секунд вместо 3
```

---

## Структура файла

```
Preloader.tsx
├── Imports (строки 1-5)
├── Interface (строки 7-9)
├── Компонент Preloader (строки 11-197)
│   ├── State управление (строки 12-15)
│   ├── Логика загрузки (строки 17-41)
│   ├── Render (строки 43-195)
│   │   ├── Фоновые слои (строки 50-64)
│   │   ├── Glass overlay (строки 66-68)
│   │   ├── Noise texture (строки 70-71)
│   │   ├── Верхняя полоса + текст (строки 74-108)
│   │   ├── Центральный процент (строки 111-152)
│   │   └── Нижняя полоса + текст (строки 154-190)
│   └── Export (строки 197)
```

---

## API Reference

### Props

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| `onComplete` | `() => void` | ✅ | Callback вызываемый после завершения загрузки |

### State

| State | Type | Default | Description |
|-------|------|---------|-------------|
| `progress` | `number` | `0` | Текущий прогресс загрузки (0-100) |
| `isExiting` | `boolean` | `false` | Флаг анимации выхода |
| `isReady` | `boolean` | `false` | Флаг готовности страницы |

---

## Поддержка браузеров

- ✅ Chrome/Edge (последние версии)
- ✅ Firefox (последние версии)
- ✅ Safari (последние версии)
- ✅ Mobile browsers (iOS Safari, Chrome Mobile)

---

## Производительность

### Оптимизация

1. **Сжатие изображений:** Используйте WebP формат для фонов
2. **Размер изображений:** Не более 1920x1080 для фуллскрин фонов
3. **Interval:** Не уменьшайте `intervalTime` ниже 16ms

### Рекомендуемые размеры изображений

- **Desktop:** 1920x1080 или 2560x1440
- **Mobile:** 1080x1920 (вертикальная ориентация)
- **Формат:** WebP или PNG
- **Размер файла:** До 500KB на изображение

---

## Лицензия

MIT License - свободно используйте и модифицируйте под свои нужды.

---

## Автор

Designed by **Salim Sokurov**

---

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории проекта.

**Приятной работы! 🚀**
